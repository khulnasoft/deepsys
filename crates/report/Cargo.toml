[package]
name = "report"
version = "0.1.0"
edition = "2024"

[dependencies]
deepsys-types = { path = "../types" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
